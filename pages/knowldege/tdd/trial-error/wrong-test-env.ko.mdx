# 시행착오
import Callout from 'nextra-theme-docs/callout'

## A. 문제
최신 버전의 Jest를 설치하고 `$ npx jest` 하니까 에러가 발생했다.  
<Callout type="error" emoji="🚫">
  Consider using the "jsdom" test environment.
</Callout>

## B. 원인
Jest 28 버전 이후로 "jsdom"이 기본 제공되지 않기에 **따로 설치해야**한다고 한다.

## C. 해결
```bash
npm i -D jest-environment-jsdom
```
그리고 *jest.config.js* 에다  
`testEnvironment: 'jsdom',`  
을 추가한다.

- - -
발생일: 2022.10.31  
키워드: wrong test env, Jest 29, jsdom, 버전 이슈

